/* Initiate Config fields*/
import {LinkAttributes} from "../models/linkAttributes";
import {Desk} from "../models/desk";

export class Config {
    url: string;
    apiKey: string;
    params: string[]
/*
    desks: Desk[];
*/
}